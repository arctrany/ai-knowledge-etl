name: knowledge-etl
version: 0.1.3
description: >
  Extract-Transform-Load pipeline for knowledge extraction.
  URLs, images, PDFs, directories, git repos → pure text Markdown.
  Supports crawling with depth, relevance scoring, and output transformation
  to skill, plugin, prompt, RAG formats.
author: haowu
license: MIT

components:
  skills:
    - extract
  commands:
    - extract
  agents:
    - extractor
    - crawler-coordinator
    - crawler-summarizer
    - output-transformer

keywords:
  - browser
  - web scraping
  - knowledge extraction
  - content extraction
  - playwright
  - crawl
  - rag
  - skill generation

dependencies:
  mcp:
    - "@playwright/mcp"

links:
  documentation: https://github.com/microsoft/playwright-mcp
